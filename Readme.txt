完成了以下内容：
Level-0：

1.给你一个AI算法（mnist），你将其发布在应用程序服务器上，接受用户提交的请求（图片），然后将其识别出来；

2.将识别出来的结果返回给用户，将图片信息保存到NoSQL数据库中；

Level-1

3. 将整个代码部署到容器里

在demo video中演示了接受图片并识别的功能，然后返回给用户结果，并将图片与结果存在NoSQL里。
然后演示了这个程序是在docker里运行的，（command：docker run -d -p 80:80 fashion_mnist）
因为录制时运行docker会导致电脑运行很慢，故只演示了docker运行完之后的效果。
在演示结束之后停止了这个containers，刷新之后显示了无法连接的页面，证明其是在docker里运行的。